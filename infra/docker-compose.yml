services:
  nginx:
    build:
      context: ../app
      dockerfile: ../infra/nginx.Dockerfile
    image: nginx
    container_name: nginx
    ports:
      - "8080:80"
    restart: always

  # node:
  #   # stuff

  # sqlite:
  #   # stuff
